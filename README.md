# sveltekit-authjs-twitter

Running locally:

```shell
yarn install
yarn dev
```

Building:

```shell
yarn install
yarn build
```

Running with nodejs:

```shell
node build/index.js
```

Running with express:

```shell
node express.js
```
